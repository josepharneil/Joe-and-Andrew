using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RL_TimerManager : MonoBehaviour
{
    
    // Start is called before the first frame update
    

    /*//public TimerState timerState = TimerState.NotYetActive;

    // Update is called once per frame
    void OnTriggerEnter2D(Collider2D col)
    {
        timer.StartTimer();
    }

    void Update()
    {
        switch (timerState)
        {
            case TimerState.NotYetActive:
                break;
            case TimerState.Running:
                //something to run the timer
                break;
                //does sometthing to either move the player or reset the timer
        }
    }*/
}
